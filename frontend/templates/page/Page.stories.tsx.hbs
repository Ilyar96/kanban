import type { Meta, StoryObj } from '@storybook/react';
import {{pascalCase pageName}} from './{{pascalCase pageName}}';

const meta: Meta<typeof {{pascalCase pageName}}> = {
  title: 'pages/{{pascalCase pageName}}',
  component: {{pascalCase pageName}},
  parameters: {
    layout: 'fullscreen',
  },
  tags: [],
  argTypes: {},
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {
  args: {},
};
