import { classNames } from 'shared/lib/classNames/classNames';
import { memo } from 'react';
import cls from './{{pascalCase entityName}}.module.scss';

interface {{pascalCase entityName}}Props {
  className?: string;
}

export const {{pascalCase entityName}} = memo(({ className }: {{pascalCase entityName}}Props) => {
  return (
    <div className={classNames(cls.{{camelCase entityName}}, {}, [className])}>
      {/* Содержимое {{pascalCase entityName}} */}
    </div>
  );
});
