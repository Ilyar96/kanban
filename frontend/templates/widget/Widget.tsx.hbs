import { classNames } from '@/shared/lib/classNames/classNames';
import { memo } from 'react';
import cls from './{{pascalCase widgetName}}.module.scss';

interface {{pascalCase widgetName}}Props {
  className?: string;
}

export const {{pascalCase widgetName}} = memo(({ className }: {{pascalCase widgetName}}Props) => {
  return (
    <div className={classNames(cls.{{camelCase widgetName}}, {}, [className])}>
      {/* Содержимое {{pascalCase widgetName}} */}
    </div>
  );
});
