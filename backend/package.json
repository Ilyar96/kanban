{
	"name": "backend",
	"version": "1.0.0",
	"description": "Kanban backend API with auth, roles, boards, columns, tasks and invitations",
	"main": "src/server.js",
	"scripts": {
		"db:check": "node src/scripts/check-db.js",
		"dev": "npm run db:check && npm run prisma:generate && nodemon src/server.js",
		"start": "npm run db:check && npm run prisma:deploy && node src/server.js",
		"prisma:generate": "prisma generate",
		"prisma:deploy": "npm run prisma:generate && prisma migrate deploy",
		"prisma:migrate": "prisma migrate dev",
		"prisma:studio": "prisma studio"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "commonjs",
	"dependencies": {
		"@prisma/client": "^6.16.2",
		"bcryptjs": "^3.0.3",
		"cors": "^2.8.6",
		"dotenv": "^17.4.2",
		"express": "^5.2.1",
		"helmet": "^8.1.0",
		"jsonwebtoken": "^9.0.3",
		"morgan": "^1.10.1",
		"nodemailer": "^9.0.1",
		"prisma": "^6.16.2",
		"swagger-jsdoc": "^6.2.8",
		"swagger-ui-express": "^5.0.1",
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"nodemon": "^3.1.14"
	}
}
